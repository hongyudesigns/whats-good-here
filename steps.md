# What's Good Here — Change Log

This file is a running log of meaningful changes to the app. Each entry should be short, dated, and reference the checklist items it implements.

## Entries

- 2026-03-14 — Cursor redesign v3 plan drafted (UI overhaul, background gradient, typography system, torn receipt, history pills, and error states). (Planning only; no runtime code changes.)
- 2026-03-14 — Implemented V3.1–V3.3 typography foundation: swapped layout to DM Sans + Poppins via `next/font/google`, mapped Tailwind `--font-sans` and `body` to the DM Sans UI font, and added a `.receipt-font` helper class for Poppins-based receipt content.
- 2026-03-14 — Implemented V3.4–V3.6 background shell: replaced warm paper background with a fixed full-viewport layered blue radial gradient using the four specified blues at 50% opacity, tuned the grain overlay for the new palette, and kept the centered 390px shell layout unchanged.
- 2026-03-14 — Implemented V3.8/V3.9 core layout states: split the page into a top content region and a fixed bottom search bar, added a `useKeyboardOffset` hook based on `visualViewport`, and applied a translateY offset so the bar stays 12px above the keyboard on mobile while remaining bottom-pinned on desktop.
- 2026-03-14 — CF.1: Restaurant category filtering. In `app/api/search/route.ts`, added `RESTAURANT_CATEGORY_KEYWORDS` and `hasRestaurantCategory()`; in `loadBusinesses()` we now keep only rows whose `categories` contain at least one of the keywords, so search results are limited to food/restaurant businesses.
- 2026-03-14 — CF.2: Error message position. Moved the error message from below the search bar to directly below the "What's good here?" headline; styled as white text, 14px, DM Sans Regular. Removed error display from the search bar area.
- 2026-03-14 — CF.3: Search bar placeholder. Default placeholder changed to "What restaurant are you at?"; "Search other restaurant" still used when a receipt card is visible.
- 2026-03-14 — CF.4: Idle subtext replaced with the "personal crusade" welcome copy; DM Sans Regular 14px, white at 80% opacity, centered, 24px horizontal margin.
- 2026-03-14 — CF.5: Search bar position raised; bottom padding set to max(10vh, 16px + safe-area) so the bar sits in the bottom 10% of the viewport with more room above the home indicator. Keyboard-aware behavior unchanged.
- 2026-03-14 — CF.6: Torn paper edges. Removed CSS pseudo-element torn effect from globals.css. Added inline SVG at top and bottom of receipt card: white (#FFFFFF), 16px tall, full width, irregular zigzag paths (top and bottom use different paths). SVGs overlap card with -mb-px / -mt-px. Idle welcome text set to left-aligned.
- 2026-03-14 — CF.7: Previous search pills. Added SearchHistoryEntry type and searchHistory state (max 10). On successful receipt we prepend and dedupe by businessId. When a receipt is showing, a horizontally scrollable pill row appears at the top (16px + safe-area); pills use pill styling from brief; tap loads that receipt from cache (no API). Row hidden when no history. Scrollbar hidden via .hide-scrollbar.
- 2026-03-14 — CF.8: Receipt printing loader. Updated loader so skeleton receipt now feeds down from top using `receiptFeedDown` (clip-path from 0 → full height). Content order: name+address skeleton, star+review skeleton, A-List skeleton, Blacklist skeleton. Audio behavior unchanged (printer.mp3 while feeding, tear.mp3 on completion). Headline changed to left-aligned. Added logging of full Claude dish-name lists and deduped lists, plus per-business Claude name caching via `getClaudeDishesForBusiness` so repeated searches for the same business reuse names with temperature=0 for determinism.
- 2026-03-14 — Five fixes (next batch): (1) Headline "What's good here?" reverted to centered; body text stays left-aligned. (2) Loading gap fixed by removing receipt card fade-in so results show immediately when loading completes. (3) Torn-edge SVGs: removed rotate-180 so top jagged edge faces down into card and bottom jagged edge faces up into card. (4) Receipt card border-radius set to 0 in .receipt-card; 12px kept in comment; removed rounded-xl from loader. (5) Claude dish-names cache cleared before each receipt request so fresh Claude results and logs can be inspected (e.g. Fish and Chips for The Dandelion); no hardcoded fallback.
- 2026-03-14 — Four fixes (next batch): (1) Loading animation reworked to height-based: wrapper uses max-height 0→420px (receiptGrowDown 1.2s), overflow hidden, content at full height inside; receipt stays visible until results replace it. (2) Torn edges: rotate-180 restored on both SVGs; receipt card border-radius 0 (no rounding); -mb-1/-mt-1 for flush overlap. (3) Fish and Chips logs: user to run Dandelion search and paste terminal output. (4) Per-session cache re-enabled: removed claudeDishNamesCache.clear().
- 2026-03-14 — Loading state and receipt animation (Step 1): Removed skeleton receipt loader entirely. No skeleton, no spinner. Deleted `ReceiptPrintingLoader` component, `useRotatingMessage`, and `loadingMessages`. During loading the page now shows only the headline “What’s good here?” and the static gradient (breathing gradient added in Step 2).
- 2026-03-14 — Loading state and receipt animation (Step 2): Breathing gradient during loading (v1). Used body class "loading" and animated CSS custom properties driving radial-gradient background-image; positions and opacity shifted in a 3.5s ease-in-out loop.
- 2026-03-14 — Loading state and receipt animation (Step 3): When results arrive, gradient settles (body "loading" class removed). Receipt wrapped in div with overflow-hidden and receiptGrowDown 1.2s cubic-bezier(0.32, 0, 0.27, 1) forwards. Keyframe max-height 0 to 600px on mount; content at full size, only wrapper height animates, card reveals downward from top.
- 2026-03-14 — Breathing gradient animation (complete rethink): Replaced background-image gradients with a fixed full-viewport `.gradient-layer` behind the shell, rendering three large blurred blue blobs plus a permanent white-peak blob at the top center. In `page.tsx`, the layer is always present; when `status === "loading"` it receives `gradient-layer--loading`, which drives smooth `transform` + `opacity` animations on each blue blob via `blobDriftSlow`, `blobDriftMedium`, and `blobDriftFast` keyframes. After the latest tune, blobs now drift ~20–25% with opacity between 0.2 and 0.9 at 4s/3s/2s durations (Aurora-level motion), and the white peak is a 400px, opacity-1.0 blob at the top center so it’s clearly visible.
- 2026-03-14 — Previous search pill entrance animation: The newest history pill (index 0) now animates in with a dot→pill→label sequence. Added `.pill-enter` and `.pill-enter__label` classes: the pill scales from 0.1→1.1→1.0 on the X-axis with a springy cubic-bezier, and the label fades in after a short delay. Wired in `page.tsx` by applying `pill-enter` / `pill-enter__label` to the first pill when history is rendered.
- 2026-03-14 — Claude cache cleared for Deviled Eggs debugging: At the top of the `GET` handler in `app/api/search/route.ts`, added `claudeDishNamesCache.clear()` so every request goes back to Claude for fresh dish-name extraction. This avoids stale per-session cache when re-checking dishes like Deviled Eggs or Fish and Chips in the A-List.
- 2026-03-14 — Background/debug fixes: (1) Added a debug `useEffect` in `page.tsx` that logs when `status === "loading"` and whether `.gradient-layer` has the `gradient-layer--loading` class; also used a temporary red blob override during early tuning (removed after). (2) Updated previous-search pills to render whenever `searchHistory.length > 0` and either a receipt is visible or `status === "loading"`, so pills stay visible during loading. (3) Switched the receipt card shadow from a rectangular `box-shadow` to `filter: drop-shadow(0 8px 24px rgba(0,0,0,0.12))` so the shadow follows the torn SVG edges more subtly.
- 2026-03-14 — Background gradient v2: switched to 5 moving blur blobs (including white). Replaced prior CSS keyframe drift with `requestAnimationFrame` movement in `app/page.tsx`: during `status === "loading"` each blob independently bounces across the gradient-layer bounds, updating position every frame and cycling through the palette on boundary collisions; when loading ends, a return mode eases all blobs back to their resting transforms smoothly before stopping the loop. Also updated `app/globals.css` to remove the old blob drift keyframes and keep blob styling purely visual.
- 2026-03-14 — Major redesign prep (Steps 1–3): (Step 1) Removed `claudeDishNamesCache.clear()` from `GET` in `app/api/search/route.ts` so Claude dish-name cache persists for the server session. (Step 2) Removed all blob/gradient-layer/rAF background code from `app/page.tsx` and all blob CSS from `app/globals.css`; replaced background with the exact requested radial Tailwind class on the main viewport wrapper and replaced loading state with a centered white CSS spinner. (Step 3) Installed `@radix-ui/react-tooltip`, `@radix-ui/react-dialog`, and `framer-motion`.
- 2026-03-14 — Major redesign (Steps 4–7): Added `app/components/ui/ai-prompt-box.tsx` from the provided PromptInputBox implementation and adapted it for app integration. Replaced the old fixed bottom search bar in `app/page.tsx` with `PromptInputBox` using frosted glass overrides, safe-area bottom padding, and placeholder switching between `"What restaurant are you at?"` and `"Search other restaurant"`. Wired `onSend` to existing search flow with restaurant-forced query handling; removed old keyboard-offset bar positioning hook usage from page layout. Replaced the PromptInputBox bottom-left icon cluster/dividers with horizontally scrollable previous-search pills (frosted style, hidden scrollbar, 32px height, 14px horizontal padding, 8px gap) and hooked pill taps to in-memory session cache loading. Updated headline readability on the warm gradient with a subtle text shadow.
- 2026-03-14 — Twelve-fix polish pass: moved the warm radial gradient to the full-viewport outer wrapper in `app/layout.tsx` and kept the 390px app shell transparent with 24px horizontal padding. Removed top-of-screen pills and moved pills into a separate fixed row directly above the bottom prompt box with 8px gap; added loading pill skeleton (`shimmer-pulse`) that appears during search and transitions to real pills when results load. Updated prompt styling to dark mode (`#1F2023` background, `#444444` border, white text, 50% white placeholder), bottom wrapper margins to 24px, and removed paperclip/upload UI from `PromptInputBox`. Set receipt shadow to subtle `drop-shadow(0 4px 12px rgba(0,0,0,0.10))`.
- 2026-03-14 — Five-fix pass (1/5): WordLoader text color updated from white to `#1A1A1A` in `app/components/ui/word-loader.tsx` and reinforced from `page.tsx` className so loader matches headline/body dark text tone.
- 2026-03-14 — Five-fix pass (2/5): Pills above the search box lightened to reduce competition with the dark prompt box: background `#3A3A3E` and border `#555555` for both real history pills and loading skeleton pill.
- 2026-03-14 — Five-fix pass (3/5): WordLoader position moved from inline content flow to a fixed centering container that occupies the available vertical space between the headline region and the bottom prompt/pills region, so loading copy sits in the true visual middle of the content area.
- 2026-03-14 — Five-fix pass (4/5): Synchronized loading loops to a 2s cadence: `shimmer-pulse` updated to 2s in `globals.css`, gradient pulse already runs at 2s, and `WordLoader` GSAP timeline now normalizes each full loop to ~2s via timeline `timeScale`.
- 2026-03-14 — Five-fix pass (5/5): Added Claude cache debug logs in `app/api/search/route.ts` (`Claude cache size` at GET start and `Claude cache stored for` after cache set). Triggered a Zahav request against local dev server; terminal showed `GET /api/search?name=Zahav 404` so request did not hit the search route logic and cache logs did not emit in this run.
- 2026-03-19 — Six fixes pass (1/6): Slowed loading timings. Normalized `WordLoader` full loop to ~5s, updated `shimmer-pulse` to 3s, and updated `gradientPulse` (loading-only) to 4s for a gentler feel.
- 2026-03-20 — Six fixes pass (2/6): Repositioned `WordLoader` to midpoint between headline and pills; updated multiple/dish option cards to dark pill styling; softened pill colors; removed red loading border from `PromptInput`; set search placeholder to `"Searching..."` during loading.
- 2026-03-20 — Reverted UI to frosted glass (white theme): search box, pills, option/error cards, headline/body copy, and WordLoader text moved back to frosted white styling; updated prompt textarea placeholder opacity and microphone/send button contrast.
- 2026-03-20 — Check-in: new request queued (solid white UI + locked/100vh no-scroll layout + slower/larger WordLoader). Starting implementation with fix #1 only next.
- 2026-03-20 — Fix #1: removed frosted glass. Search box + pills + option/error cards now use solid white (`#FFFFFF`) with dark text (`#1A1A1A`) and light borders (`rgba(0,0,0,0.1)`). Updated prompt textarea placeholder to `#6B6B6B` and ensured Mic/Send button icon contrast on white.
- 2026-03-20 — Fix #2: enforced no-scroll + strict viewport fit. Set `html, body` to `height: 100%` and `overflow: hidden`, changed layout containers from `min-h-screen` to `h-screen`, and constrained `app/page` main to `h-[100vh] overflow-hidden`.
- 2026-03-20 — Fix #3: locked bottom search box position by removing `10vh` from the `paddingBottom` calculation (now only `env(safe-area-inset-bottom)`), so it doesn't shift when viewport height changes.
- 2026-03-20 — Fix #4: updated `WordLoader` size and speed (text 1.5x bigger; full loop normalized to ~10s via GSAP `timeScale`).
- 2026-03-20 — Fix #3/#4: removed all voice recording from `ai-prompt-box.tsx` (VoiceRecorder/isRecording removed) and replaced Mic/Square/StopCircle controls with a single ArrowRight send button.
- 2026-03-20 — Fix #1 (search box position): raised bottom padding to `calc(env(safe-area-inset-bottom) + 32px)` so the fixed search box has more breathing room.
- 2026-03-20 — Fix #2 (search box styling): set background `rgba(255,255,255,0.8)`, `border-2 border-white`, `backdrop-blur-[8px]`, text `#1A1A1A`.
- 2026-03-20 — Fix #5: send ArrowRight button renders only when typing (`input.trim() !== ''`) and fades in smoothly (AnimatePresence + motion).
- 2026-03-20 — Check-in: new request queued for search-box position/styling + removing voice recording and using ArrowRight send button (visible only while typing with fade-in).
- 2026-03-20 — Check-in: queued four fixes for independent scrollable middle content + compact inline search + bottom fade mask. Starting with fix #1 (scrollable middle) next.
- 2026-03-20 — Fix #1: created independent scrollable middle content area in `app/page.tsx` (flex + `overflow-y-auto`) while keeping headline and “We found a few places…” fixed at top; added higher z-index to the fixed bottom pills/search so they sit above scrolling cards.
- 2026-03-20 — Fix #2/#3: compacted the search box and moved the ArrowRight send button inline with the textarea (removed the extra icon/action row; reduced padding inside `ai-prompt-box.tsx` to keep ~52px empty height).
- 2026-03-20 — Fix #4: added bottom fade mask overlay to the scrollable middle content area in `app/page.tsx` so list items fade near the fixed pills/search region.

 - 2026-03-20 — Check-in: new fixes queued (phantom space below search box in idle + fixed 360px list scroll cutoff). Starting with phantom space removal first.
- 2026-03-20 — Fix #1 (phantom space): limited the bottom fade overlay to only list states (`multiple`, `dish_results`) so idle/neutral state no longer shows an extra white band under the fixed search box.
- 2026-03-20 — Fix #2 (list height): constrained the middle scrollable container to `h-[360px]` for `multiple`/`dish_results` so it hard-cuts cleanly at ~4.5 cards and doesn’t bleed into the fixed pills/search area.
- 2026-03-20 — Check-in: queued section labels in `app/page.tsx` plus four fixes (dock 56px offset, header `mb-6`, send-button alignment, and API multiple-match sorting by `review_count`).
- 2026-03-20 — Added explicit section labels in `app/page.tsx`: `{/* HEADER SECTION */}`, `{/* CONTENT SECTION */}`, and `{/* DOCK */}`.
- 2026-03-20 — Dock position update: increased bottom dock padding to `calc(env(safe-area-inset-bottom) + 56px)`.
- 2026-03-20 — Header spacing update: added `mb-6` to the HEADER SECTION `<header>` in `app/page.tsx`.
- 2026-03-20 — Send button alignment update: in `ai-prompt-box.tsx`, aligned the inline send control vertically (`items-center`) and added `pr-2` so it sits ~8px from the right edge.
- 2026-03-20 — API multiple-match sort update: in `app/api/search/route.ts`, sorted `matches` by `review_count` descending before returning top 10 for `status: "multiple"`.
- 2026-03-20 — Two small fixes: centered idle personal-crusade body text in `app/page.tsx`; in `ai-prompt-box.tsx`, kept input enabled during loading while showing disabled send button styling (`opacity-50`, non-clickable) until loading completes.
- 2026-03-20 — Four fixes: removed temporary phone outline wrapper from `app/layout.tsx`; added `themeColor: "#483E65"` metadata and `html { background-color: #483e65; }` for mobile status/safe-area blending; enabled vertical scrolling in results state by making main overflow conditional and rendering dock in normal flow when receipt is visible; applied 32px receipt spacing via top/bottom margins around the receipt container.
- 2026-03-20 — Fix #1 (status bar blending): kept `themeColor: "#483E65"` and set both `html` + `body` background colors to `#483E65` in `globals.css` so safe areas/status bar blend with top gradient color.
- 2026-03-20 — Fix #2 (results scroll): in `app/page.tsx`, results state now uses visible middle content flow (not 360px clipped list), keeps dock in document flow, and applies a 32px receipt-to-dock gap so receipt → gap → pills/search can be scrolled on mobile.
- 2026-03-20 — Fix #3 (PWA metadata): updated `app/layout.tsx` with manifest link, Apple web app metadata (`capable`, `black-translucent`), and `viewport-fit=cover`; added `public/manifest.json` (`What's Good Here` / `WGH`, standalone, `#483E65` theme/background).
- 2026-03-20 — Fix #4 (global Claude cache): moved `claudeDishNamesCache` in `app/api/search/route.ts` to a `globalThis.__claudeCache` map so cache persists across Next.js dev HMR restarts.
- 2026-03-20 — Two fixes: updated `ai-prompt-box.tsx` textarea colors to white typed text + `rgba(255,255,255,0.6)` placeholder; added `pb-20` to results-state main scroll container in `app/page.tsx` so dock/search remains fully visible at bottom of scroll.
- 2026-03-20 — Three fixes: aligned dock pills/search widths to receipt edges by removing extra dock `px-6` in `app/page.tsx`; made previous-search pills fully rounded (`rounded-full`); updated safe-area blending in `globals.css` using `html` linear-gradient (`#483E65` top → `#FFE483` bottom) with transparent body background.
- 2026-03-20 — Check-in: queued layout fixes for dock margins in all states, forced yellow bottom safe-area bar, repaired multiple-match structure/spacing, and global 32px section-gap consistency.
- 2026-03-20 — Several fixes: restored dock `px-6` in all states; added fixed bottom safe-area yellow overlay (`#FFE483`) in `layout.tsx`; updated multiple-match vertical structure in `page.tsx` (header top-start for this state, subtitle at 32px, fixed 360px list region, 32px list→dock spacing); standardized major section gaps to 32px using shared `mb-8`/`mt-8` spacing patterns.
- 2026-03-21 — Loading typing behavior hardening: kept prompt input editable during loading but blocked submit actions while `isLoading` (including Enter key path) in `app/components/ui/ai-prompt-box.tsx`, so users can type ahead without firing overlapping requests.
- 2026-03-21 — Layout spacing pass in `app/page.tsx`: (1) moved idle headline/body block to top-aligned `pt-8` (matching multiple-matches vertical position), (2) reduced headline→idle-body spacing to `mb-4` (16px), (3) restored section breathing room in list states by keeping 32px separation before the dock (`mb-8` under multiple/dish result lists), and (4) removed dock displacement by using one consistently fixed bottom dock across states (no results-state movement/slide behavior).
- 2026-03-21 — Multiple-matches header grouping update in `app/page.tsx`: moved the `"We found a few places..."` subtitle into the header block (section 1), set headline→subtitle spacing to `mt-4` (16px), and set subtitle→scrollable list spacing to `mt-8` (32px) while keeping the list→dock gap at `mb-8`.
- 2026-03-21 — Keyboard auto-dismiss on search submit: in `app/components/ui/ai-prompt-box.tsx`, `handleSubmit` now blurs the active element before sending when input/files are present, so mobile keyboards dismiss immediately for both send-button taps and Enter-key submissions.
- 2026-03-21 — Zahav truncation hardening in `app/api/search/route.ts`: increased Claude `max_tokens` in `getClaudeDishNames()` from `512` to `1024` to reduce mid-JSON truncation risk; added parse fallback so if full-response `JSON.parse()` fails, the code attempts to salvage and parse the first JSON-array span (`[` ... last `]`). If fallback parse still fails, logs warning details and returns `null` as before.
- 2026-03-21 — Results-state spacing fix in `app/page.tsx`: restored a clear 32px receipt-to-dock gap by applying `mb-8` on the receipt wrapper and increasing results-mode bottom scroll padding (`pb-28`) so the gap remains visible above the fixed pills/search dock.
- 2026-03-21 — Results overlap + loading headline alignment follow-up in `app/page.tsx`: increased results scroll container bottom padding to `pb-32` so receipt content fully clears the fixed pills/search dock (no overlap), while keeping the receipt wrapper `mb-8` for explicit 32px clearance; unified non-receipt header positioning to always use top-aligned `pt-8` (including loading/error/multiple/idle), with header still hidden only when a receipt result card is shown.
- 2026-03-21 — Results dock flow refactor in `app/page.tsx`: restored non-fixed, in-flow dock in receipt/results state so layout is now receipt card → `mb-8` gap → pills row → `mb-4` gap → search box → bottom `pb-8` (safe-area aware). Fixed-bottom dock behavior is preserved for idle/loading/multiple/error states only. Receipt card dimensions/content unchanged.
- 2026-03-21 — Results dock motion + Safari clearance pass in `app/page.tsx`: removed results-state receipt wrapper grow animation that was causing pills/search to slide downward during result transition; added adaptive results-scroll bottom padding via `padding-bottom: calc(env(safe-area-inset-bottom) + 130px)` on the main results container so the search box clears mobile Safari browser UI while degrading gracefully on desktop (`env(...) = 0`).
- 2026-03-21 — Spacing/padding tune in `app/page.tsx`: set multiple-matches list container to symmetric vertical spacing with `my-8` (32px above and below) so it sits equidistant between header and dock; reduced results-state scroll bottom padding from `calc(env(safe-area-inset-bottom) + 130px)` to `calc(env(safe-area-inset-bottom) + 65px)`.
- 2026-03-21 — Multiple-list fade mask rewrite in `app/page.tsx`: removed broken top/bottom gradient overlay elements and replaced with a single scroll-container `mask-image`/`-webkit-mask-image` linear gradient driven by scroll state. Logic now applies bottom-only fade at top, top-only fade at bottom, and both-edge fades in the middle; no mask when list does not overflow.
- 2026-03-21 — Multiple-list fade softness tuning in `app/page.tsx`: reduced mask fade zones to ~40px per edge and softened transitions using intermediate alpha stops (`0.7` at mid-fade) so the top/bottom fades remain readable indicators without obscuring as much list content.
- 2026-03-21 — Updated idle-state body copy in `app/page.tsx` to: “Harnessing the power of 4-5 star reviews so you never sit at your table and wonder what this place is famous for.”

